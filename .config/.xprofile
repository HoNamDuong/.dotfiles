#!/bin/bash

# Adds ~/.local/bin and its subdirectories to $PATH
export PATH="${PATH}:${HOME}/.local/bin"

# XDG paths
export XDG_CACHE_HOME="$HOME/.cache"
export XDG_CONFIG_HOME="$HOME/.config"
export XDG_DATA_HOME="$HOME/.local/share"
export XDG_STATE_HOME="$HOME/.local/state"

# Default program
export EDITOR="nvim"
export VISUAL="nvim"
export TERMINAL="alacritty"
export BROWSER="google-chrome-stable"

# Ibus
export GTK_IM_MODULE="ibus"
export QT_IM_MODULE="ibus"
export XMODIFIERS="@im=ibus"

# Other
export QT_QPA_PLATFORMTHEME="qt5ct"
export ZDOTDIR="$HOME/.config/zsh"
export BAT_THEME="base16"
export RIPGREP_CONFIG_PATH="$HOME/.config/ripgrep/.ripgreprc"
export TIMEFMT=$'\n\nCPU\t%P\nuser\t%*U\nsystem\t%*S\ntotal\t%*E'
# export GTK_THEME="Orchis-Dark-Compact"

# Monitor config
[ -f ~/.screenlayout/single.sh ] && . ~/.screenlayout/single.sh &
# [ -f ~/.screenlayout/dual.sh ] && . ~/.screenlayout/dual.sh &
# Enable numlock
[ -f /usr/bin/numlockx ] && numlockx &
# Set a delay of 500ms and a rate of 40Hz
[ -f /usr/bin/xset ] && xset r rate 500 40 &
# Disable the screensaver
[ -f /usr/bin/xset ] && xset s off && xset s noblank && xset -dpms &
# Set locker utility
[ -f /usr/bin/xss-lock ] && [ -f /usr/bin/slock ] && xss-lock /usr/bin/slock &
# Network Manager Applet
[ -f /usr/bin/nm-applet ] && nm-applet &
# # Blueman Applet
[ -f /usr/bin/blueman-applet ] && blueman-applet &
# Set randomize wallpaper every 10 minutes
bash -c '[ -f ~/.local/bin/randomwallpaper ] && while true; do ~/.local/bin/randomwallpaper && sleep 10m || break ; done' &
# Load config nvidia-settings
[ -f /usr/bin/nvidia-settings ] && nvidia-settings --load-config-only &
# Ibus Daemon
[ -f /usr/bin/ibus-daemon ] && ibus-daemon -rxRd &
# Authentication
[ -f /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 ] && /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1 &
# Clipboard manager
[ -f /usr/bin/copyq ] && sh -c "sleep 1 && copyq --start-server" &
# # Compositor
# [ -f /usr/bin/picom ] && picom -b &
