#!/usr/bin/bash

checkout() {
    local branches branc
    branches=$(git branch) &&
    branch=$(echo "$branches" | fzf-tmux -r 20% --reverse) &&
    git checkout $(echo "$branch" | sed "s/.* //")
}

checkout
